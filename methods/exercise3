def multiply(num1,num2)
  num1 * num2
end

puts "What is the first number to multiply?"
factor1 = gets.chomp.to_i
puts "What is the second number to multiply?"
factor2 = gets.chomp.to_i

puts "The result is #{multiply(factor1,factor2)}"